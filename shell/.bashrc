# ================================
# Bash config
# ================================

# Load shared config
[ -f "$HOME/.shell-common" ] && source "$HOME/.shell-common"
[ -f "$HOME/.fzf-common" ] && source "$HOME/.fzf-common"

